import React from "react";
import CurtainLoginDemo from "../../components/demo/CurtainLoginDemo";

/**
 * Demo Page for Curtain Login Animation
 *
 * Access at: http://localhost:3000/demo/curtain
 *
 * This is a standalone demo that doesn't affect your main app.
 * Test it, and if you like it, we can integrate it into AuthPage.
 */

const CurtainDemoPage: React.FC = () => {
  return <CurtainLoginDemo />;
};

export default CurtainDemoPage;
